name: Rate Limits
type: module
description: Provides arbitrary rate limits per route.
configure: entity.rate_limit_config.collection
dependencies:
  - drupal:system (>=8.5)
core_version_requirement: ^9.1 || ^10
